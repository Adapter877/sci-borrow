name: Release

on:
  release:
    types: [published]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-20.04
    permissions:
      contents: read
      packages: write
      id-token: write
    outputs:
      image_tag: ${{ steps.meta.outputs.tags }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3  # เช็คเอาท์ repository ปัจจุบัน

      - name: Install cosign
        if: github.event_name != 'pull_request'
        uses: sigstore/cosign-installer@v3.1.1
        with:
          cosign-release: 'v2.1.1'  # ติดตั้ง cosign เวอร์ชัน 2.1.1

      - name: Setup Docker buildx
        uses: docker/setup-buildx-action@v2  # ตั้งค่า Docker buildx

      - name: Log into registry ${{ env.REGISTRY }}
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}  # ล็อกอินเข้าสู่ Docker registry

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@98669ae865ea3cffbcbaa878cf57c20bbf1c6c38
        with:
          images: ${{ env.REGISTRY }}/$(echo ${{ env.IMAGE_NAME }} | tr '[:upper:]' '[:lower:]')
          tags: |
            type=sha,prefix={{branch}}-,format=short  # สร้างแท็กโดยใช้ SHA ของ commit ปัจจุบัน

  deploy:
    needs: build
    runs-on: ubuntu-20.04

    steps:
      - name: Setup Git
        run: |
          git config --global user.name "Github Actions"
          git config --global user.email "actions@github.com"  # ตั้งค่า Git

      - name: Checkout gitops repository
        uses: actions/checkout@v3
        with:
          repository: Adapter877/sci-borrow
          ref: main
          token: ${{ secrets.GH_PAT }}  # เช็คเอาท์ repository สำหรับ gitops

      - name: Update image tag for UAT
        if: ${{ contains(github.event.release.tag_name, '-beta') }}
        env:
          REGISTRY: ghcr.io
          IMAGE_NAME: $(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')
          IMAGE_TAG: ${{ needs.build.outputs.image_tag }}
        run: |
          echo "Updating image tag to: $IMAGE_TAG"
          IMAGE_TAG_CLEANED=$(echo $IMAGE_TAG | sed 's/^.*-//')
          echo "Cleaned image tag: $IMAGE_TAG_CLEANED"
          yq eval ".spec.template.spec.containers[0].image = \"$REGISTRY/$IMAGE_NAME:$IMAGE_TAG_CLEANED\"" -i deployment/deployment.yaml
          git add deployment/deployment.yaml
          git commit -m "Set 'web' image tag to '${IMAGE_TAG_CLEANED}'"
          git push  # อัปเดต image tag ในไฟล์ deployment สำหรับ UAT

      - name: Check deployment file content
        run: cat deployment/deployment.yaml  # ตรวจสอบเนื้อหาของไฟล์ deployment หลังจากอัปเดต